#include "Main/Uniform.hpp"

namespace Main {
    namespace Uniform {
        GLuint phase;
        GLuint windowWidth;
        GLuint windowHeight;
        GLuint perspectiveMatrix;
        GLuint offset;
    }
}
