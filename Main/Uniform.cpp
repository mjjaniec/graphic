#include "Main/Uniform.hpp"

namespace Main {
    namespace Uniform {
        GLuint windowWidth;
        GLuint windowHeight;
        GLuint modelToWorldMatrix;
        GLuint worldToCameraMatrix;
        GLuint cameraToClipMatrix;
    }
}
